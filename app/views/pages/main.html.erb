<header>
  <div class="container">
    <div class="intro-text">
      <div class="intro-lead-in">Now playing:<span class="now-playing-blurb">Tested by Andy</span></div>
    </div>
    <div class="video">
      <div class="video-player"></div>
    </div>

    <% if !user_signed_in? %>
      <%= link_to 'I want my 15 minutes!', new_user_registration_path, class: 'btn btn-xl' %>
    <% end %>
  </div>
</header>

